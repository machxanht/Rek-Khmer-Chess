# Browser WebSocket endpoint. Use wss:// when the web app is served over HTTPS.
VITE_REK_WS_URL=ws://localhost:8787

# Online server process settings. Export these in the hosting platform/shell.
REK_WS_HOST=0.0.0.0
REK_WS_PORT=8787
