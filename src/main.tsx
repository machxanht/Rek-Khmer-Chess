import React from 'react'
import ReactDOM from 'react-dom/client'
import { PixelPerfectApp } from './PixelPerfectApp'
import './pixel-perfect.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PixelPerfectApp />
  </React.StrictMode>,
)
